#include "monty.h"
/**
 * f_stack -outputs the top
 * @head: head stack
 * @counter: line_number
 * Return: no return value
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
